#include<stdio.h>
#include<conio.h>
int main()
{
char c;
printf("enter character");
scanf("%d",&c);
if((c>='a'&&c<='z')||(c>'A'&&(C>'Z'))
{
printf("%c is alphabet");
}
else
{
printf({"%c ic not alphabet");
}
return 0;
}
